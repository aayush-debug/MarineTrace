{
  "name": "marinetrace-workspace",
  "version": "1.0.0",
  "private": true,
  "description": "MarineTrace - AI-Powered Marine Oil-Spill Detection & Vessel Attribution Platform",
  "scripts": {
    "start": "bash start.sh",
    "dev": "bash start.sh",
    "backend": "cd backend && source venv/bin/activate && uvicorn app.main:app --reload --port 8000",
    "frontend": "npm --prefix frontend run dev",
    "build": "npm --prefix frontend run build",
    "lint": "npm --prefix frontend run lint",
    "test": "cd backend && PYTHONPATH=. ./venv/bin/pytest tests/"
  }
}
